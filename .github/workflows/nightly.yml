name: Nightly

# Every merge into `nightly` publishes a rolling pre-release that Nightly users
# auto-update from. Same gate as CI: tests and lint run first, and the publish
# step depends on them, so a red test never reaches a Nightly user either.
#
# Nightly releases all share one tag (`nightly`) and are ordered by CI run
# number rather than version, because several can share a commit count.
on:
  push:
    branches: [nightly]
    paths:
      - "apps/desktop/**"
      - ".github/workflows/nightly.yml"
      - ".github/scripts/**"
      - ".swiftlint.yml"
  workflow_dispatch:

concurrency:
  group: nightly
  cancel-in-progress: false

defaults:
  run:
    working-directory: apps/desktop

jobs:
  test:
    name: Test (swift test)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v5
      - name: Configure git identity
        run: |
          git config --global user.email "ci@example.com"
          git config --global user.name "Coppice CI"
      - name: Run tests
        run: swift test

  lint:
    name: Quality (SwiftLint)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v5
      - run: brew install swiftlint
      - name: Lint
        working-directory: .
        run: swiftlint --strict --reporter github-actions-logging

  publish:
    name: Build and publish nightly
    runs-on: macos-26
    needs: [test, lint]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Compute version
        run: |
          echo "VERSION=0.$(git rev-list --count HEAD)" >> "$GITHUB_ENV"
          echo "BUILD=${{ github.run_number }}" >> "$GITHUB_ENV"

      - name: Set up stable code-signing certificate
        env:
          MACOS_SIGN_CERT_P12: ${{ secrets.MACOS_SIGN_CERT_P12 }}
          MACOS_SIGN_CERT_PASSWORD: ${{ secrets.MACOS_SIGN_CERT_PASSWORD }}
        run: bash "$GITHUB_WORKSPACE/.github/scripts/setup-signing.sh"

      - name: Build app and installer
        run: |
          export COPPICE_VERSION="$VERSION"
          export COPPICE_BUILD="$BUILD"
          export COPPICE_CHANNEL=nightly
          ./build.sh
          ./make-dmg.sh

      # One rolling tag. Delete and recreate so the `nightly` tag always points
      # at the newest pre-release and the updater has a single feed to follow.
      - name: Publish pre-release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete nightly --yes --cleanup-tag 2>/dev/null || true
          gh release create nightly build/Coppice-Nightly.dmg \
            --title "Coppice Nightly $VERSION (build $BUILD)" \
            --notes "Rolling pre-release from \`nightly\` at ${GITHUB_SHA:0:7}. Build $BUILD." \
            --prerelease \
            --target "$GITHUB_SHA"

      - name: Bump Homebrew cask
        env:
          TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
        run: |
          if [ -z "${TAP_TOKEN:-}" ]; then
            echo "::warning::TAP_TOKEN not set — skipping cask bump."
            exit 0
          fi
          VERSION="$VERSION" CASK=coppice-nightly \
            bash "$GITHUB_WORKSPACE/.github/scripts/bump-cask.sh" "$PWD/build/Coppice-Nightly.dmg"
